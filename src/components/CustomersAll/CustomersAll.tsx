import React from 'react';
import CustomersList from '../CustomersList/CustomersList';

type Props = {};

const CustomersAll = (props: Props) => {
  return (
    <div>
      <CustomersList />
    </div>
  );
};

export default CustomersAll;
